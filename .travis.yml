language: python
# python:
  # We don't actually use the Travis Python, but this keeps it organized.
  - "3.6"
matrix:
  include:
  - os: osx
    env: CONDA=3.6
  - os: osx
    env: CONDA=3.7
  - os: linux
    env: CONDA=3.6
  - os: linux
    env: CONDA=3.7

install:
  - sudo apt-get update
  # We do this conditionally because it saves us some downloading if the
  # version is the same.
  - if [ "$TRAVIS_OS_NAME" = linux ]; then
      MINICONDAVERSION="Linux"
    else
      MINICONDAVERSION="MacOSX"
    fi
  - wget https://repo.continuum.io/miniconda/Miniconda3-latest-$MINICONDAVERSION-x86_64.sh -O miniconda.sh;
  - bash miniconda.sh -b -p $HOME/miniconda
  - export PATH="$HOME/miniconda/bin:$PATH"
  - hash -r
  - conda config --set always_yes yes --set changeps1 no
  - conda config --add channels conda-forge
  - conda update -q conda
  # Useful for debugging any issues with conda
  - conda info -a

  # Replace dep1 dep2 ... with your dependencies
  - conda create -q -n test-environment python=$CONDA cython pytables pytest rdkit
  - source activate test-environment
  - python setup.py install

script:
  pytest
